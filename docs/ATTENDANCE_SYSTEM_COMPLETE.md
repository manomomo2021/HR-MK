# نظام الحضور والانصراف الاحترافي المتكامل

## نظرة عامة

تم إنشاء نظام حضور وانصراف احترافي جديد بالكامل يتصل مباشرة بجهاز البصمة ويعرض البيانات بطريقة احترافية وجذابة.

## 🎯 **الميزات الرئيسية**

### **1. الاتصال المباشر بجهاز البصمة:**
- 🔗 **اتصال مباشر** بجهاز ZKTeco (IP: 192.168.0.201)
- 📡 **جلب البيانات الفعلية** من الجهاز
- 🔄 **تحديث تلقائي** مع زر التحديث
- ⚡ **معالجة ذكية** للسجلات المكررة

### **2. واجهة احترافية متطورة:**
- 🎨 **تصميم عصري** مع تأثيرات حركية
- 📊 **إحصائيات تفاعلية** في الوقت الفعلي
- 🔍 **بحث وفلترة متقدمة**
- 📱 **تصميم متجاوب** لجميع الشاشات

### **3. أوضاع عرض متعددة:**
- 📅 **اليوم**: عرض بيانات يوم محدد
- 📋 **جميع البيانات**: عرض جميع السجلات
- 📊 **شهري**: عرض بيانات شهر محدد

## 📊 **الإحصائيات المعروضة**

### **كروت الإحصائيات:**
```
📅 تاريخ اليوم: 15/01/2024
👥 عدد الحضور: 25 موظف
🚪 عدد الانصراف: 23 موظف
⏱️ إجمالي الإضافي: 45 ساعة 30 دقيقة
⏰ إجمالي التأخير: 2 ساعة 15 دقيقة
```

### **إحصائيات الموظف الفردية:**
```
👤 أحمد محمد السعيد
📈 إحصائيات الحضور والانصراف

✅ أيام الحضور: 22 يوم
❌ أيام الغياب: 3 أيام
📊 نسبة الحضور: 88.0%
⏱️ إجمالي الإضافي: 15 ساعة 30 دقيقة
⏰ إجمالي التأخير: 45 دقيقة
🕐 متوسط ساعات العمل: 8.5 ساعة
```

## 🎨 **التصميم والواجهة**

### **1. الألوان التفاعلية:**
- 🟢 **أخضر**: الحضور المسجل
- 🔴 **أحمر**: عدم تسجيل الحضور أو التأخير
- 🟡 **أصفر**: عدم تسجيل الانصراف
- 🔵 **أزرق**: الانصراف المسجل
- 🟣 **بنفسجي**: الوقت الإضافي

### **2. التأثيرات الحركية:**
```javascript
// تأثيرات الدخول
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}

// تأثيرات التمرير
whileHover={{ scale: 1.05, y: -5 }}

// تأثيرات الجدول
transition={{ delay: i * 0.05 }}
```

### **3. الأيقونات والرموز:**
- 🔍 **البحث**: fas fa-search
- 📅 **التاريخ**: fas fa-calendar-day
- 👥 **الحضور**: fas fa-user-check
- 🚪 **الانصراف**: fas fa-sign-out-alt
- ⏱️ **الوقت الإضافي**: fas fa-clock
- ⏰ **التأخير**: fas fa-exclamation-triangle

## 🔧 **الميزات التقنية**

### **1. معالجة البيانات الذكية:**
```javascript
// تصفية السجلات المكررة
records = records.filter((rec, i) => {
  if (i === 0) return true
  return (rec.timestamp - records[i - 1].timestamp) > 60 * 1000
})

// حساب الوقت الإضافي
const endOfDay = new Date(inRecord.timestamp)
endOfDay.setHours(16, 30, 0, 0)
if (outRecord.timestamp > endOfDay) {
  overtimeMinutes = Math.floor((outRecord.timestamp - endOfDay) / (1000 * 60))
}

// حساب التأخير
const startLimit = new Date(inRecord.timestamp)
startLimit.setHours(8, 10, 0, 0) // 8:10 صباحاً (10 دقائق سماح)
if (inRecord.timestamp > startLimit) {
  lateMinutes = Math.floor((inRecord.timestamp - startLimit) / (1000 * 60))
}
```

### **2. تصدير Excel متقدم:**
```javascript
const exportData = filtered.map(row => ({
  'كود الموظف': row.userId,
  'اسم الموظف': row.userName,
  'الراتب': `${row.salary} ج.م`,
  'التاريخ': row.date,
  'وقت الحضور': row.inTime,
  'وقت الانصراف': row.outTime,
  'مدة العمل': row.workDuration,
  'الوقت الإضافي': row.overtime,
  'التأخير': row.late,
  'طريقة التحقق': row.verifyMode
}))
```

### **3. معالجة الأخطاء المحسنة:**
```javascript
// معالجة عدم وجود المكتبة
if (!ZKLib) {
  return res.status(500).json({
    success: false,
    message: 'مكتبة node-zklib غير مثبتة. يرجى تثبيتها باستخدام: npm install node-zklib',
    installCommand: 'npm install node-zklib'
  })
}

// معالجة أخطاء الاتصال
if (error.code === 'ECONNREFUSED') {
  errorMessage = 'لا يمكن الاتصال بجهاز البصمة. تأكد من أن الجهاز متصل بالشبكة'
} else if (error.code === 'ETIMEDOUT') {
  errorMessage = 'انتهت مهلة الاتصال بجهاز البصمة'
}
```

## 📱 **أدوات التحكم**

### **1. أزرار العرض:**
```
[📅 اليوم] [📋 جميع البيانات] [📊 شهري]
```

### **2. حقول البحث:**
```
🔍 بحث باسم الموظف: [أحمد محمد]
📅 اختيار التاريخ: [15/01/2024]
📊 اختيار الشهر: [2024-01]
```

### **3. أزرار الإجراءات:**
```
[🔄 تحديث البيانات] [📊 تصدير Excel]
```

## 📊 **الجدول التفاعلي**

### **رؤوس الأعمدة:**
```
كود الموظف | اسم الموظف | الراتب | التاريخ | الحضور | الانصراف | مدة العمل | الإضافي | التأخير | طريقة التحقق
```

### **مثال على البيانات:**
```
101 | أحمد محمد | 8,000 ج.م | 15/01/2024 | 08:00 AM | 05:30 PM | 9 ساعة 30 دقيقة | 60 دقيقة | 0 دقيقة | بصمة إصبع
102 | فاطمة علي | 6,500 ج.م | 15/01/2024 | 08:15 AM | 04:30 PM | 8 ساعة 15 دقيقة | 0 دقيقة | 15 دقيقة | بصمة وجه
```

### **ملخص الجدول:**
```
إجمالي السجلات: 125 | الحضور: 120 | الانصراف: 115 | متأخرين: 15 | وقت إضافي: 45
```

## 🔄 **تحديث البيانات**

### **التحديث التلقائي:**
```javascript
const fetchAttendanceData = async () => {
  try {
    setLoading(true)
    const res = await fetch('/api/attendance')
    const result = await res.json()
    
    if (!result.success) {
      throw new Error(result.message || 'تعذر جلب البيانات من جهاز البصمة')
    }
    
    setAttendance(result.attendance || [])
    toast.success('✅ تم تحديث بيانات الحضور من جهاز البصمة')
  } catch (err) {
    setError(err.message)
    toast.error('❌ ' + err.message)
  } finally {
    setLoading(false)
  }
}
```

### **التحديث اليدوي:**
```javascript
const refreshData = async () => {
  setIsRefreshing(true)
  await fetchAttendanceData()
  setIsRefreshing(false)
}
```

## 📈 **تصدير البيانات**

### **تقرير Excel شامل:**
```
تقرير الحضور والانصراف
نظام إدارة الموارد البشرية
تاريخ الإنشاء: 15/01/2024
إجمالي السجلات: 125

كود الموظف,اسم الموظف,الراتب,التاريخ,وقت الحضور,وقت الانصراف,مدة العمل,الوقت الإضافي,التأخير,طريقة التحقق
101,أحمد محمد السعيد,8000 ج.م,2024-01-15,08:00 AM,05:30 PM,9 ساعة 30 دقيقة,60 دقيقة,0 دقيقة,بصمة إصبع
```

## 🚀 **الفوائد العملية**

### **للموارد البشرية:**
1. **بيانات دقيقة** مباشرة من الجهاز
2. **إحصائيات شاملة** لكل موظف
3. **تقارير جاهزة** للتصدير
4. **واجهة سهلة** الاستخدام

### **للمحاسبين:**
1. **ربط مباشر** بالرواتب
2. **حساب الوقت الإضافي** تلقائياً
3. **تتبع التأخير** والغياب
4. **تقارير مالية** دقيقة

### **للإدارة:**
1. **مراقبة فورية** للحضور
2. **إحصائيات يومية** وشهرية
3. **تحليل الأداء** والانضباط
4. **قرارات مبنية** على بيانات دقيقة

## 📞 **الاستخدام**

### **للوصول للنظام:**
1. انتقل إلى `/attendance`
2. ستظهر البيانات المباشرة من جهاز البصمة
3. استخدم أدوات الفلترة والبحث
4. انقر على اسم الموظف لرؤية إحصائياته

### **للتصدير:**
1. اختر الفترة المطلوبة
2. انقر على "تصدير Excel"
3. احفظ الملف للمراجعة

### **نصائح للاستخدام:**
- **تأكد من اتصال الجهاز** بالشبكة
- **حدث البيانات** بانتظام
- **راجع الإحصائيات** يومياً
- **صدر التقارير** للأرشفة

---

## 🎉 **النتيجة النهائية**

نظام الحضور والانصراف أصبح:
- ✅ **متصل مباشرة** بجهاز البصمة
- ✅ **احترافي في التصميم** مع واجهة جذابة
- ✅ **دقيق في الحسابات** للوقت الإضافي والتأخير
- ✅ **شامل في التقارير** مع تصدير Excel
- ✅ **سهل الاستخدام** مع فلترة متقدمة
- ✅ **متكامل مع النظام** لربط الرواتب

النظام جاهز للاستخدام المهني المتقدم! 🚀
